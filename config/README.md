# Config Examples

These files preserve useful config structure salvaged from the master archive.
They are **example-only** and are not currently wired into the executable runtime.

Files:
- `runtime_settings.example.yaml`
- `evaluation_config.example.yaml`
- `coefficients_registry.example.yaml`
- `strategy_variants.example.yaml`

Use them as doctrine/backlog inputs when promoting more of the archive into typed runtime configuration.
