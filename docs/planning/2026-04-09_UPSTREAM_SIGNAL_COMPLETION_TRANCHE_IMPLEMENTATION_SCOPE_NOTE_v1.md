# 2026-04-09_UPSTREAM_SIGNAL_COMPLETION_TRANCHE_IMPLEMENTATION_SCOPE_NOTE_v1

Status: closed support note retained as evidence under the upstream tranche pack closed through Gate 252 in the uploaded workspace copy.

## Purpose

Checkpoint extension authority: `/mnt/data/checkpoint_integrity_normative_extension.md` applies to modified upstream-tranche boundaries and gate-local tests.


Freeze the bounded intent of the upstream signal completion tranche as closed evidence after Gate 252 closeout.

## This tranche is

- one upstream ingress-promotion tranche for live market-regime and participation truth;
- one bounded promotion of already-lawful Class B families into the prepared-runtime and cognition-ingress path;
- one tranche that honours `pyproject.toml`, current SQLite-first defaults, and the repo's typed runtime contracts.

## This tranche is not

- a redesign of the corrected main serial stack;
- a new desk-cognition stage or hidden inference lane;
- a DMP / DMP V2 stage-order change;
- a history-lane feedback path;
- a breadth/concentration/dealer-flow vendor-acquisition programme;
- an optimisation, allocator, or exit-strategy tranche.

## Frozen first-implementation scope

- cross-asset regime promotion for ES, NQ, SOX, VIX, VVIX, US10Y, US2Y, and USDJPY
- same-bucket participation / liquidity baseline promotion for volume, spread, and trade-count when lawfully available
- raw-to-cognition wiring for `MarketRegimeContextInput`
- participation-supporting ingress promotion into lawful Step 1 / Step 3 supporting surfaces
- optional top-of-book quote truth only if already cheaply available in the admitted source path

## Explicit deferred scope

- breadth primitives
- concentration primitives
- dealer-flow raw drivers for `vanna_proxy` / `charm_proxy`
- any broader capture rewrite required to acquire Class A families

## Required promotion boundary

- prepared-runtime truth must become richer before cognition ingress becomes richer
- one lawful source path must be named for each promoted family
- `relative_volume_ratio` must not remain an ambiguous toy proxy if same-bucket baseline truth is promoted
- optional top-of-book promotion must be either explicitly admitted and cheap or explicitly deferred

## Routing rule

This scope note is retained support material under the closed pack. It remains consistent with repo-root `PLANS.md`, the canonical gate map, the leaves ledger, and the execution log after Gate 252 closeout.
