# 2026-04-06_SLIM_ACTIVE_REPO_CUTOVER_AND_SUBSTANTIVE_TEST_AUDIT_BOOTSTRAP_RUNTIME_SURFACE_AUDIT_READ_TRIGGER_AND_AUTHORITY_ADOPTION_v1

## Purpose

Adopt the rescoped `07_RUNTIME_SURFACE_OWNERSHIP_AND_DOWNSTREAM_CONSUMER_LEDGER` in the slim successor repo and freeze how the first substantive test audit must read it.

## Adopted specialised authority

The successor repo must adopt the rescoped `07` ledger as:
- a specialised runtime authority ledger;
- not a universal front-door doctrine file; and
- not part of the baseline frozen reading order for every task.

`AGENTS.md` must mirror that posture explicitly so Codex does not demote the file into operator-only memory.

## Mandatory read-trigger for this pack

The first substantive test audit must read the adopted `07` ledger whenever a retained test touches any of the following:
- stage packet versus workflow packet authority;
- compatibility surface or compatibility carriage law;
- downstream runtime reader permissions;
- replay, bounded-trace, or review seam interpretation;
- API compatibility wrappers that preserve older read shapes over newer canonical runtime truth.

## Successor audit consequences

- tests that assert `final_risk_join` without regard to preserved seam surfaces must be classified against the `07` prohibited-inference rules;
- tests that rely on `session_clock` must be classified as compatibility-wrapper tests rather than canonical temporal-state tests;
- tests that reconstruct Stage 6 candidate ownership from Stage 5 admissibility alone must be treated as stale or normatively wrong;
- review-packet duplicate carriage must not outrank separately carried authoritative surfaces during test interpretation.

## Required AGENTS.md behavioural bridge

`AGENTS.md` in the successor repo must tell Codex to read `docs/07_RUNTIME_SURFACE_OWNERSHIP_AND_DOWNSTREAM_CONSUMER_LEDGER.md` whenever work touches any of the mandatory read-trigger domains listed above.
It must also state that `docs/07...` is specialised authority, not a baseline front-door read for unrelated planning-only tasks.

## What this adoption does not mean

- it does not claim that runtime semantics changed in the cutover;
- it does not change the frozen authority order in `docs/01` through `docs/06`;
- it does not make the `07` ledger a required read for unrelated planning-only tasks.

## What later work must not do

- classify a runtime-surface test from old chat memory rather than the adopted `07` ledger;
- treat compatibility carriage as co-equal with separately carried authoritative surfaces;
- use the rescoped `07` adoption as cover for unplanned runtime rewrites.

## Gate 218 verification against the installed successor repo

Gate 218 verified the successor repo's currently installed doctrine rather than reusing the source-repo claim from chat memory.

- the installed `docs/07_RUNTIME_SURFACE_OWNERSHIP_AND_DOWNSTREAM_CONSUMER_LEDGER.md` contains the rescoped rewrite markers introduced for the successor repo: `## 0. Classification and read-trigger`, `## 1.1 Relation to the wider doc stack`, and `## 1.2 Maintenance law`
- the installed `AGENTS.md` states that `docs/07...` is a specialised runtime authority ledger and carries the mandatory read-trigger domains for runtime surface ownership, stage packet versus workflow packet authority, compatibility surface or compatibility carriage law, downstream runtime reader permissions, replay or bounded-trace or review seam interpretation, and API compatibility wrappers
- the retained-surface inventory frozen in Gate 218 is now the baseline input set for later audit classification, so runtime-surface tests can be classified against the actual successor repo rather than against an abstract cutover brief

This Gate 218 verification binds the later audit to the installed successor doctrine surfaces only. It does not claim that runtime semantics changed merely because the rewritten `07` ledger and the matching `AGENTS.md` read-trigger were already adopted in Gate 217.
