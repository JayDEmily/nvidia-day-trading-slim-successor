# Remaining-docs promotion recommendation

## Bottom line
The next implementation-facing move from the remaining PDFs is **not** a new trading module.
It is to strengthen existing review, carry, and overlay surfaces with bounded inputs already supported by the repo.

## Recommended immediate promotions
1. Keep the extracted data rows as `fixture_candidate` or `needs_recompute` only.
2. Promote **failure-pattern guardrails** into review/eval surfaces before adding more strategy logic.
3. Promote **Asia/Europe precursor context** and **macro-volatility confirmation** only as bounded feature families or overlays.
4. Treat the SLV origin passages as doctrine and explanation support, not as evidence that more executable SLV logic already existed.

## Not recommended right now
- direct runtime promotion of screenshot/OCR numeric values
- live-strategy claims based on legacy conversation text
- importing identity/career or interface digressions into backlog

## One recommended next implementation-facing move
Use the remaining-doc extraction to deepen:
- carry replay event/weekend splits,
- review packets over execution records,
- and module-attribution doctrine.

That path converts the strongest surviving legacy value into deterministic audit surfaces without importing fantasy implementation claims.
