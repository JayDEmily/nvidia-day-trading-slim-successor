# BUILD PLAN

## Status

This file is archived context only.

It is not an active execution authority.

## Current authority

Use these instead:
- repo-root `PLANS.md`
- the active planning quartet under `docs/planning/`
- `docs/01_NORMATIVE.md` for authority and precedence rules

## Why this file still exists

The repo keeps it as historical context for the earlier build sequence.
If it conflicts with the active planning set, the active planning set wins.
